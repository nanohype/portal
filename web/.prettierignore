# Generated by openapi-typescript (npm run generate:api) — byte-identity with the
# generator's output is asserted by the CI "API contract drift" gate, so prettier
# must never touch it.
src/api/types.ts

# Build + coverage artifacts
dist/
coverage/

# Lockfile is npm's to format
package-lock.json
